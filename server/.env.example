# 数据库连接串（Postgres）
DB_URL=postgres://user:password@localhost:5432/spribe
# JWT 签名密钥（生产环境务必换成足够长的随机字符串）
JWT_SECRET=change-me-to-a-long-random-secret
# 服务监听端口
PORT=4000
# 允许的前端来源白名单（逗号分隔）。上线时在此追加真实域名，如 https://admin.yourdomain.com
CORS_ORIGIN=http://localhost:5173,http://localhost:5174
