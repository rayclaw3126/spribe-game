# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key

# Local Claude Code settings
.claude/settings.local.json

# Debug / verification screenshots dump — 过程产物，不入库
# （shots/ 里全是截图，非 src 引用的资产图；如需入库某张用 git add -f）
shots/*.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
._*
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
